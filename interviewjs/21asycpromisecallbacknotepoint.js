// in the desing we are using prototyping for connection creation between the pages moving form one page to the another page --------------in the desing create conncetion through the prototype

//create connection between the functions by using the function return results , to the other function through the parameters -----------in the function connection with callback
// we can create a connection between the two functions by calling seperately with the help of arugument


// ----------------
// synchronous
// AsyNCHRONOUS
// run immdietly on the same Time

// run with some time delay on the backgeound not on the same seperare thread

// if it run on the same thread it will block the below code when it will take time

// inisde promise Code
// async function code run on the same thread

// when it will call the await

// whrn it will call the .then() then the function becaome the asynchronosu

// ---------------------------------------------------------------------------

// -------------------------------synchronous----------------------------------

// -------------------------------Asynchronous----------------------------------

// call will run the program
//call the name will run and also if we pass the name to the function function arugument will create connection between the arugument name and the function which is accepting the aruguments

//get data from the network

//show contact feom the form

//show footer data

// js engine will start when we are goin to call js file to run on the browser (page call is main thread)
//call stack is responsibe fo execute code line by line
//global execute context (GEc)
//EC
//web api it will hadle time delay
//call back quee
//event loop eventloop will chek on the callback quee
//// -------------------------------synchronous----------------------------------

// -------------------------------Asynchronous----------------------------------

// call will run the program
//call the name will run and also if we pass the name to the function function arugument will create connection between the arugument name and the function which is accepting the aruguments

//get data from the network

//show contact feom the form

//show footer data

// js engine will start when we are goin to call js file to run on the browser (page call is main thread)
//call stack is responsibe fo execute code line by line
//global execute context (GEc)
//EC
//web api it will hadle time delay
//call back quee
//event loop eventloop will chek on the callback quee
//


// All Callback function are not Async functions
// Only SetTimeout only Async


// let promisedata=new Promise((resolve,reject)=>{

 
//      for (let i = 0; i <= 1000000000; i++) {
//     if (i == 1000000000) {
//       resolve("we get the final data 1000000000");
//       // console.log(1000000000);
//     }
//   }
//   reject((err)=>{
// console.log(err)
//   })
  
// })
// console.log(promisedata);
// console.log(":SLVEFKM;V;")

// 1)WHEN PROMISE WILL ACT synchronous 
// 2)when promise will act asynchronous
// promise this.state.  resolve ,reject ,pensing
// promise result
// promise type

// video 
// 1)promise syntxa then we will get promise object 
// 2)promise inside function and outside function 
// 3)based on our consditon we will call resolve and reject 
// 4)promise create state pening
// 5)sucess and resolve will take function as arugument also (we can pass any tyep of data but only single arugument)
// 6)


// 21 callback-callback hell
// 22 promise
// 23 async and await 
// 24 get
// 25 post
// 26 update
// 27 delete

// when we get promise object on that time then or catch will called (for the fulfilled or)

// all members are practice with brains 
// with out using any perfect tools
// when we practicw with brain it will create pressre